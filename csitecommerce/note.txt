CREATE DATA IF NOT EXISTS csitcommerce;

use csitecommerce

CREATE TABLE IF NOT EXISTS users(
uid int AUTO_INCREMENT PRIMARY key,
name varchar(100),
email varchar(100) UNIQUE,
password varchar(100),
gender ENUM('male','female','others'),
role SET('admin','user'),
image varchar(100),
created_at datetime,
update_at date_time_set
);